#* @openeo-process
load_collection <- function(id,
                            spatial_extent = NULL,
                            temporal_extent = NULL,
                            bands = NULL,
                            properties = NULL) {
  data <- "mock data"
  data
}

#* @openeo-process
save_result <- function(data, format, options = NULL) {
  return(TRUE)
}
