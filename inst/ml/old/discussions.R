
# create functions poc
# export_cube(data, name, folder)
export_cube <- function(data, name, folder) {
}

# import_cube(name, folder)
import_cube <- function(name, folder) {
}


cube_regularize <- function(datacube) {
}

#ml_class_random_forest(num_trees, max_variables, seed)
ml_class_random_forest <- function(num_trees, max_variables, seed) {
}

#ml_fit(model, training_set, target)
ml_fit <- function(model, training_set, target) {
}

ml_predict <- function(data, model, dimensions) {
}
