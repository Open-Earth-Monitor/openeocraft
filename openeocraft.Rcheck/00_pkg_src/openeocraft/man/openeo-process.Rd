% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decorators.R
\name{openeo-process}
\alias{openeo-process}
\title{Register an openEO process from \verb{#* @openeo-process}}
\usage{
`openeo-process`(api, expr, ..., file)
}
\arguments{
\item{api}{API object.}

\item{expr}{Parsed expression; expected to be an assignment whose RHS is a
\code{function(...) { ... }}. The LHS must be a symbol (the openEO process id).}

\item{...}{Reserved for future decorator arguments (passed from \verb{#*}
line tokens after \verb{@openeo-process}).}

\item{file}{Path to the processes R source file; used to resolve
\verb{dirname(file)/processes/}.}
}
\value{
\code{NULL}, invisibly.
}
\description{
Reads or creates \verb{processes/<process_id>.json} in a directory \strong{next to}
\code{file} (\verb{dirname(file)/processes/<id>.json}), merges it into the API process
list via \code{\link[=add_process]{add_process()}}. The process \code{id} and JSON file name equal the
assigned function name (LHS of \verb{<-}).
}
\details{
\strong{JSON lifecycle:} If \verb{<id>.json} is missing, a minimal descriptor is
written from the function's formal arguments: names become parameter \code{name}
fields; formals matching \code{\link[=empty_name]{empty_name()}} are required (\code{optional: false}),
others are optional with \code{default} taken from the formal's default value.
Production deployments should ship hand-written JSON (summary, description,
schemas, returns) and treat auto-generation as a bootstrap only.

The implementation assigns the function into the API namespace when the
processes file is sourced (before decorator registration); this handler only
registers metadata for \verb{/processes}.

Throws from \code{\link[=stopifnot]{stopifnot()}} if the \code{processes} directory cannot be created or
the JSON file is missing after write.
}
\seealso{
\code{\link[=process_decorators]{process_decorators()}}, \code{\link[=load_processes]{load_processes()}}, \code{\link[=add_process]{add_process()}}
}
\keyword{internal}
